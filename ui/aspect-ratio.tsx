'use client';

import * as AspectRatioPrimitive from '@radix-ui/react-aspect-ratio';

/**
 * Hold a box at a ratio. Adapted from shadcn/ui (MIT); it is a re-export,
 * exactly as upstream ships it, because there is nothing to style.
 */
export const AspectRatio = AspectRatioPrimitive.Root;
